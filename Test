local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/shammanpok3-cmyk/Rivals-/refs/heads/main/UElib"))()

local t = Library.Theme

if t then
    t.Accent              = Color3.fromRGB(80, 80, 80)
    t.Background          = Color3.fromRGB(10, 10, 10)
    t.Header              = Color3.fromRGB(15, 15, 15)
    t.TextColor           = Color3.fromRGB(220, 220, 220)
    t.SubTextColor        = Color3.fromRGB(140, 140, 140)
    t.TabBackground       = Color3.fromRGB(18, 18, 18)
    t.TabActive           = Color3.fromRGB(50, 50, 50)
    t.TabInactive         = Color3.fromRGB(22, 22, 22)
    t.GroupBackground     = Color3.fromRGB(14, 14, 14)
    t.GroupBorder         = Color3.fromRGB(40, 40, 40)
    t.ToggleBackground    = Color3.fromRGB(25, 25, 25)
    t.ToggleEnabled       = Color3.fromRGB(90, 90, 90)
    t.ToggleDisabled      = Color3.fromRGB(30, 30, 30)
    t.Outline             = Color3.fromRGB(35, 35, 35)
    t.Divider             = Color3.fromRGB(30, 30, 30)
    t.ScrollBar           = Color3.fromRGB(60, 60, 60)
    t.ScrollBarBackground = Color3.fromRGB(18, 18, 18)
end

local imgUrl   = "https://res.cloudinary.com/w8q7jrey/image/upload/v1789048821/Wallpaper%20backgrounds/Owner%20background.jpg"
local folder   = "Secret/Backgrounds"
local cached   = folder .. "/Owner_background.jpg"

if not isfolder("Secret") then makefolder("Secret") end
if not isfolder(folder) then makefolder(folder) end

local function getWin()
    local sg = Library.ScreenGui
    if not sg then return nil end
    for _, v in ipairs(sg:GetChildren()) do
        if v:IsA("Frame") and v.Visible and v.AbsoluteSize.X > 300 then
            return v
        end
    end
end

local function applyBg(id)
    local win = getWin()
    if not win then return end
    for _, v in ipairs(win:GetChildren()) do
        if v.Name == "sbg" or v.Name == "sov" then v:Destroy() end
    end
    local img = Instance.new("ImageLabel")
    img.Name                   = "sbg"
    img.BackgroundTransparency = 1
    img.Image                  = id
    img.Size                   = UDim2.fromScale(1, 1)
    img.ZIndex                 = 0
    img.ScaleType              = Enum.ScaleType.Crop
    img.Parent                 = win
    local ov = Instance.new("Frame")
    ov.Name                    = "sov"
    ov.BackgroundColor3        = Color3.new(0, 0, 0)
    ov.BackgroundTransparency  = 0.55
    ov.BorderSizePixel         = 0
    ov.Size                    = UDim2.fromScale(1, 1)
    ov.ZIndex                  = 1
    ov.Parent                  = win
end

local function fetchBg()
    if isfile(cached) then delfile(cached) end
    local ok, raw = pcall(game.HttpGet, game, imgUrl)
    if not ok or type(raw) ~= "string" or #raw < 200 then
        Library:Notify("bg failed", 4)
        return
    end
    writefile(cached, raw)
    if getcustomasset then
        applyBg(getcustomasset(cached))
        Library:Notify("bg loaded", 3)
    else
        Library:Notify("no getcustomasset", 4)
    end
end

local function stripAlpha(root)
    if not root then return end
    for _, v in ipairs(root:GetDescendants()) do
        if v:IsA("Frame") or v:IsA("ScrollingFrame") then
            if v.Name == "sbg" or v.Name == "sov" then continue end
            local s = v.AbsoluteSize
            if s.X > 120 and s.Y > 40 and v.BackgroundTransparency < 0.4 then
                v.BackgroundTransparency = 0.55
            end
        end
    end
end

local function nukeBlue(root)
    if not root then return end
    for _, v in ipairs(root:GetDescendants()) do
        if v:IsA("Frame") or v:IsA("ScrollingFrame") then
            local c = v.BackgroundColor3
            if c.B > 0.35 and c.B > c.R + 0.1 then
                local m = (c.R + c.G + c.B) / 3
                v.BackgroundColor3 = Color3.new(m * 0.6, m * 0.6, m * 0.6)
            end
        elseif v:IsA("TextLabel") or v:IsA("TextButton") then
            local tc = v.TextColor3
            if tc.B > 0.35 and tc.B > tc.R + 0.1 then
                v.TextColor3 = Color3.fromRGB(200, 200, 200)
            end
            local bc = v.BackgroundColor3
            if bc.B > 0.35 and bc.B > bc.R + 0.1 then
                v.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
            end
        elseif v:IsA("UIStroke") then
            local c = v.Color
            if c.B > 0.35 and c.B > c.R + 0.1 then
                v.Color = Color3.fromRGB(50, 50, 50)
            end
        elseif v:IsA("ImageLabel") or v:IsA("ImageButton") then
            local c = v.ImageColor3
            if c.B > 0.35 and c.B > c.R + 0.1 then
                v.ImageColor3 = Color3.fromRGB(160, 160, 160)
            end
        end
    end
end

local win = Library:CreateWindow({
    Title    = "Secret | Rivals",
    Center   = true,
    AutoShow = true,
})

local tabs = {
    main     = win:AddTab("Main"),
    visual   = win:AddTab("Visual"),
}

-- =====================================================================
--  VISUAL STATE (mirrors the config table from the visuals tab)
-- =====================================================================
local ELI = getgenv().elisium_visual or {}
getgenv().elisium_visual = ELI

-- ESP
ELI.esp = ELI.esp or {
    enable     = false,
    team_check = true,
    box = {
        enable    = false,
        color     = Color3.new(1,1,1),
        outline   = Color3.new(0,0,0),
        inline    = Color3.new(0,0,0),
        thickness = 1,
    },
    glow = {
        enable       = false,
        color_start  = Color3.new(1,1,1),
        color_end    = Color3.new(1,1,1),
        transparency = 1,
        rotation     = 90,
        speed        = 2,
        animated     = false,
    },
    filled = {
        enable       = false,
        color_start  = Color3.new(1,1,1),
        color_end    = Color3.new(1,1,1),
        transparency = 0.75,
        rotation     = 90,
        speed        = 2,
        animated     = false,
        hit_flash    = false,
        hit_color    = Color3.fromRGB(255,40,40),
        hit_time     = 0.3,
    },
    visible = { enable = false, color = Color3.fromRGB(60,255,90) },
    highlight = {
        enable              = false,
        fill_color          = Color3.fromRGB(120,81,166),
        outline_color       = Color3.new(1,1,1),
        fill_transparency   = 0.55,
        outline_transparency = 0,
    },
    health = {
        enable    = false,
        color_high = Color3.fromRGB(0,255,0),
        color_mid  = Color3.fromRGB(255,255,0),
        color_low  = Color3.fromRGB(255,0,0),
        width     = 3,
        gap       = 5,
    },
    text = {
        name  = { enable = false, color = Color3.new(1,1,1) },
        studs = { enable = false, color = Color3.new(1,1,1) },
        tool  = { enable = false, color = Color3.new(1,1,1), position = "right" },
        size  = 9,
        gradient        = false,
        gradient_color1 = Color3.fromRGB(120,81,166),
        gradient_color2 = Color3.new(1,1,1),
        flow            = false,
        flow_speed      = 1,
    },
    max_dist = 25000,
}

-- Chams
ELI.chams = ELI.chams or {
    enable           = false,
    team_check       = false,
    mode             = "highlight",
    fill_color       = Color3.fromRGB(0,170,255),
    fill_transparency = 0.5,
    outline_color    = Color3.new(1,1,1),
    outline_transparency = 0,
    material         = "Neon",
    strip_textures   = false,
    ghost            = false,
}

-- Crosshair
ELI.crosshair = ELI.crosshair or {
    enable          = false,
    style           = "cross",
    lines           = true,
    top_line        = true,
    bottom_line     = true,
    left_line       = true,
    right_line      = true,
    rotation        = 0,
    length          = 6,
    thickness       = 1,
    gap             = 3,
    color           = Color3.fromRGB(0,255,170),
    outline         = false,
    outline_color   = Color3.new(0,0,0),
    dot             = false,
    rotating        = false,
    rotating_speed  = 120,
    spread          = false,
    spread_scale    = 1,
    animation       = false,
    animation_mode  = "pulse",
    animation_speed = 3,
    fade            = false,
    fade_color      = Color3.fromRGB(255,0,170),
    follow_target   = false,
    follow_source   = "target",
    shimmer         = false,
    shimmer_speed   = 4,
    text_enable     = false,
    text_content    = "secret",
    text_font       = "UI",
    text_size       = 16,
    text_offset     = 18,
    text_color      = Color3.new(1,1,1),
    text_outline    = true,
    text_animation  = false,
}

-- Viewmodel chams
ELI.viewmodel_chams = ELI.viewmodel_chams or {
    weapon_chams = { enable = false, color = Color3.new(1,1,1), material = "Neon", transparency = 0 },
    arm_chams    = { enable = false, color = Color3.new(1,1,1), material = "Neon", transparency = 0, invisible = false },
    weapon_highlight = { enable = false, color = Color3.new(1,1,1), transparency = 0.5, outline_color = Color3.new(1,1,1), outline_transparency = 0 },
    arm_highlight    = { enable = false, color = Color3.new(1,1,1), transparency = 0.5, outline_color = Color3.new(1,1,1), outline_transparency = 0 },
}

-- Viewmodel offsets
ELI.viewmodel_offsets = ELI.viewmodel_offsets or { enable = false, x = 0, y = 0, z = 0 }

-- Material presets for chams dropdowns (same names as Eliusm)
ELI.material_names = ELI.material_names or {
    "Neon", "ForceField", "Glass", "Chrome", "Ice", "Diamond", "Hologram",
    "Ghost", "Obsidian", "Gold Foil", "Frostbite", "Plasma", "Molten", "Prism",
}

-- =====================================================================
--  MAIN TAB (kept from original secret)
-- =====================================================================
local core = tabs.main:AddLeftGroupbox("Core")
core:AddLabel("Secret online — Owner background")
core:AddToggle("ex", { Text = "Example feature", Default = false })

-- =====================================================================
--  VISUAL TAB — ported from the visuals tab, wired to Secret's UElib
-- =====================================================================
local esp_box = tabs.visual:AddLeftGroupbox("ESP")

esp_box:AddToggle("esp_enable", {
    Text = "enable",
    Default = false,
    Callback = function(v) ELI.esp.enable = v end,
})

esp_box:AddToggle("esp_team_check", {
    Text = "team check",
    Default = true,
    Callback = function(v) ELI.esp.team_check = v end,
})

esp_box:AddToggle("esp_box_enable", {
    Text = "box",
    Default = false,
    Callback = function(v)
        ELI.esp.box.enable = v
        Options.esp_box_thickness:SetVisible(v)
        Toggles.esp_glow_enable:SetVisible(v)
    end,
}):AddColorPicker("esp_box_color", {
    Title = "box color",
    Default = Color3.new(1,1,1),
    Callback = function(v) ELI.esp.box.color = v end,
}):AddColorPicker("esp_box_outline_color", {
    Title = "outline color",
    Default = Color3.new(0,0,0),
    Callback = function(v) ELI.esp.box.outline = v end,
}):AddColorPicker("esp_box_inline_color", {
    Title = "inline color",
    Default = Color3.new(0,0,0),
    Callback = function(v) ELI.esp.box.inline = v end,
})

esp_box:AddToggle("esp_glow_enable", {
    Text = "glow box",
    Default = false,
    Visible = false,
    Callback = function(v)
        ELI.esp.glow.enable = v
        Options.esp_glow_transparency:SetVisible(v)
    end,
}):AddColorPicker("esp_glow_color_start", {
    Title = "glow start",
    Default = Color3.new(1,1,1),
    Callback = function(v) ELI.esp.glow.color_start = v end,
}):AddColorPicker("esp_glow_color_end", {
    Title = "glow end",
    Default = Color3.new(1,1,1),
    Callback = function(v) ELI.esp.glow.color_end = v end,
})

esp_box:AddSlider("esp_glow_transparency", {
    Text = "glow transparency",
    Default = 1, Min = 0, Max = 1, Rounding = 2, Visible = false,
    Callback = function(v) ELI.esp.glow.transparency = v end,
})

esp_box:AddSlider("esp_box_thickness", {
    Text = "box thickness",
    Default = 1, Min = 1, Max = 5, Rounding = 0, Visible = false,
    Callback = function(v) ELI.esp.box.thickness = v end,
})

esp_box:AddToggle("esp_name_enable", {
    Text = "name",
    Default = false,
    Callback = function(v) ELI.esp.text.name.enable = v end,
}):AddColorPicker("esp_name_color", {
    Title = "name color",
    Default = Color3.new(1,1,1),
    Callback = function(v) ELI.esp.text.name.color = v end,
})

esp_box:AddToggle("esp_studs_enable", {
    Text = "distance",
    Default = false,
    Callback = function(v) ELI.esp.text.studs.enable = v end,
}):AddColorPicker("esp_studs_color", {
    Title = "distance color",
    Default = Color3.new(1,1,1),
    Callback = function(v) ELI.esp.text.studs.color = v end,
})

esp_box:AddToggle("esp_tool_enable", {
    Text = "weapon",
    Default = false,
    Callback = function(v) ELI.esp.text.tool.enable = v end,
}):AddColorPicker("esp_tool_color", {
    Title = "weapon color",
    Default = Color3.new(1,1,1),
    Callback = function(v) ELI.esp.text.tool.color = v end,
})

esp_box:AddDropdown("esp_tool_position", {
    Text = "weapon position",
    Default = "right",
    Values = { "top", "bottom", "right" },
    Callback = function(v) ELI.esp.text.tool.position = v end,
})

esp_box:AddToggle("esp_filled_enable", {
    Text = "filled",
    Default = false,
    Callback = function(v)
        ELI.esp.filled.enable = v
        Options.esp_filled_transparency:SetVisible(v)
    end,
}):AddColorPicker("esp_filled_color_start", {
    Title = "fill start",
    Default = Color3.new(1,1,1),
    Callback = function(v) ELI.esp.filled.color_start = v end,
}):AddColorPicker("esp_filled_color_end", {
    Title = "fill end",
    Default = Color3.new(1,1,1),
    Callback = function(v) ELI.esp.filled.color_end = v end,
})

esp_box:AddToggle("esp_filled_hitflash", {
    Text = "hit flash",
    Default = false,
    Callback = function(v) ELI.esp.filled.hit_flash = v end,
}):AddColorPicker("esp_filled_hitcolor", {
    Title = "hit color",
    Default = Color3.fromRGB(255,40,40),
    Callback = function(v) ELI.esp.filled.hit_color = v end,
})

esp_box:AddToggle("esp_visible_enable", {
    Text = "visible color",
    Default = false,
    Callback = function(v) ELI.esp.visible.enable = v end,
}):AddColorPicker("esp_visible_color", {
    Title = "visible color",
    Default = Color3.fromRGB(60,255,90),
    Callback = function(v) ELI.esp.visible.color = v end,
})

esp_box:AddToggle("esp_highlight_enable", {
    Text = "highlight",
    Default = false,
    Callback = function(v) ELI.esp.highlight.enable = v end,
}):AddColorPicker("esp_highlight_fill", {
    Title = "fill color",
    Default = Color3.fromRGB(120,81,166),
    Callback = function(v) ELI.esp.highlight.fill_color = v end,
}):AddColorPicker("esp_highlight_outline", {
    Title = "outline color",
    Default = Color3.new(1,1,1),
    Callback = function(v) ELI.esp.highlight.outline_color = v end,
})

esp_box:AddSlider("esp_highlight_fill_transparency", {
    Text = "highlight fill transparency",
    Default = 0.55, Min = 0, Max = 1, Rounding = 2,
    Callback = function(v) ELI.esp.highlight.fill_transparency = v end,
})

esp_box:AddSlider("esp_highlight_outline_transparency", {
    Text = "highlight outline transparency",
    Default = 0, Min = 0, Max = 1, Rounding = 2,
    Callback = function(v) ELI.esp.highlight.outline_transparency = v end,
})

esp_box:AddSlider("esp_filled_transparency", {
    Text = "fill transparency",
    Default = 0.75, Min = 0, Max = 1, Rounding = 2, Visible = false,
    Callback = function(v) ELI.esp.filled.transparency = v end,
})

esp_box:AddToggle("esp_filled_animated", {
    Text = "animated fill",
    Default = false,
    Callback = function(v)
        ELI.esp.filled.animated = v
        Options.esp_filled_rotation:SetVisible(v)
        Options.esp_filled_speed:SetVisible(v)
    end,
})

esp_box:AddSlider("esp_filled_rotation", {
    Text = "fill rotation",
    Default = 90, Min = 0, Max = 360, Rounding = 0, Visible = false,
    Callback = function(v) ELI.esp.filled.rotation = v end,
})

esp_box:AddSlider("esp_filled_speed", {
    Text = "fill speed",
    Default = 2, Min = 0, Max = 10, Rounding = 1, Visible = false,
    Callback = function(v) ELI.esp.filled.speed = v end,
})

esp_box:AddToggle("esp_health_enable", {
    Text = "health bar",
    Default = false,
    Callback = function(v)
        ELI.esp.health.enable = v
        Options.esp_health_width:SetVisible(v)
        Options.esp_health_gap:SetVisible(v)
    end,
}):AddColorPicker("esp_health_color_high", {
    Title = "health top",
    Default = Color3.fromRGB(0,255,0),
    Callback = function(v) ELI.esp.health.color_high = v end,
}):AddColorPicker("esp_health_color_mid", {
    Title = "health mid",
    Default = Color3.fromRGB(255,255,0),
    Callback = function(v) ELI.esp.health.color_mid = v end,
}):AddColorPicker("esp_health_color_low", {
    Title = "health bottom",
    Default = Color3.fromRGB(255,0,0),
    Callback = function(v) ELI.esp.health.color_low = v end,
})

esp_box:AddSlider("esp_health_width", {
    Text = "bar width",
    Default = 3, Min = 1, Max = 10, Rounding = 0, Visible = false,
    Callback = function(v) ELI.esp.health.width = v end,
})

esp_box:AddSlider("esp_health_gap", {
    Text = "bar gap",
    Default = 5, Min = 1, Max = 20, Rounding = 0, Visible = false,
    Callback = function(v) ELI.esp.health.gap = v end,
})

esp_box:AddSlider("esp_text_size", {
    Text = "text size",
    Default = 9, Min = 6, Max = 16, Rounding = 0,
    Callback = function(v) ELI.esp.text.size = v end,
})

esp_box:AddToggle("esp_text_gradient", {
    Text = "gradient text",
    Default = false,
    Callback = function(v) ELI.esp.text.gradient = v end,
}):AddColorPicker("esp_text_gradient_color1", {
    Default = Color3.fromRGB(120,81,166),
    Title = "color 1",
    Callback = function(v) ELI.esp.text.gradient_color1 = v end,
}):AddColorPicker("esp_text_gradient_color2", {
    Default = Color3.new(1,1,1),
    Title = "color 2",
    Callback = function(v) ELI.esp.text.gradient_color2 = v end,
})

esp_box:AddToggle("esp_text_flow", {
    Text = "flow animation",
    Default = false,
    Callback = function(v) ELI.esp.text.flow = v end,
})

esp_box:AddSlider("esp_text_flow_speed", {
    Text = "flow speed",
    Default = 1, Min = 0.1, Max = 5, Rounding = 1,
    Callback = function(v) ELI.esp.text.flow_speed = v end,
})

esp_box:AddSlider("esp_max_dist", {
    Text = "max distance",
    Default = 25000, Min = 100, Max = 25000, Rounding = 0, Suffix = "studs",
    Callback = function(v) ELI.esp.max_dist = v end,
})

-- =====================================================================
--  CHAMS (left side)
-- =====================================================================
local chams_box = tabs.visual:AddLeftGroupbox("Chams")

chams_box:AddToggle("chams_enable", {
    Text = "enable",
    Default = false,
    Callback = function(v) ELI.chams.enable = v end,
})

chams_box:AddToggle("chams_team_check", {
    Text = "team check",
    Default = false,
    Callback = function(v) ELI.chams.team_check = v end,
})

chams_box:AddDropdown("chams_mode", {
    Text = "type",
    Default = "highlight",
    Values = { "highlight", "chams" },
    Callback = function(v) ELI.chams.mode = v end,
})

chams_box:AddLabel("fill"):AddColorPicker("chams_fill", {
    Default = Color3.fromRGB(0,170,255),
    Transparency = 0.5,
    Callback = function(color, transparency)
        ELI.chams.fill_color = color
        ELI.chams.fill_transparency = transparency or ELI.chams.fill_transparency
    end,
})

chams_box:AddLabel("outline"):AddColorPicker("chams_outline", {
    Default = Color3.new(1,1,1),
    Transparency = 0,
    Callback = function(color, transparency)
        ELI.chams.outline_color = color
        ELI.chams.outline_transparency = transparency or ELI.chams.outline_transparency
    end,
})

chams_box:AddDropdown("chams_material", {
    Text = "material",
    Default = "Neon",
    Values = ELI.material_names,
    Visible = false,
    Callback = function(v) ELI.chams.material = v end,
})

chams_box:AddToggle("chams_strip_textures", {
    Text = "strip textures",
    Default = false,
    Visible = false,
    Callback = function(v) ELI.chams.strip_textures = v end,
})

chams_box:AddToggle("chams_ghost", {
    Text = "ghost",
    Default = false,
    Visible = false,
    Callback = function(v) ELI.chams.ghost = v end,
})

-- =====================================================================
--  CROSSHAIR (right side tabbox)
-- =====================================================================
local crosshair_tabbox = tabs.visual:AddRightTabbox()

local ch_main = crosshair_tabbox:AddTab("crosshair")
ch_main:AddToggle("crosshair_enable", {
    Text = "enabled",
    Default = false,
    Callback = function(v) ELI.crosshair.enable = v end,
}):AddColorPicker("crosshair_color", {
    Default = Color3.fromRGB(0,255,170),
    Callback = function(v) ELI.crosshair.color = v end,
})

ch_main:AddDropdown("crosshair_style", {
    Text = "style",
    Default = "cross",
    Values = { "cross", "t", "x", "circle", "dot" },
    Callback = function(v) ELI.crosshair.style = v end,
})

ch_main:AddSlider("crosshair_rotation", {
    Text = "rotation",
    Default = 0, Min = 0, Max = 360, Rounding = 0, Suffix = "°",
    Callback = function(v) ELI.crosshair.rotation = v end,
})

ch_main:AddSlider("crosshair_length", {
    Text = "length",
    Default = 6, Min = 0, Max = 40, Rounding = 0,
    Callback = function(v) ELI.crosshair.length = v end,
})

ch_main:AddSlider("crosshair_thickness", {
    Text = "thickness",
    Default = 1, Min = 1, Max = 6, Rounding = 0,
    Callback = function(v) ELI.crosshair.thickness = v end,
})

ch_main:AddSlider("crosshair_gap", {
    Text = "gap",
    Default = 3, Min = 0, Max = 20, Rounding = 0,
    Callback = function(v) ELI.crosshair.gap = v end,
})

ch_main:AddToggle("crosshair_outline", {
    Text = "outline",
    Default = false,
    Callback = function(v) ELI.crosshair.outline = v end,
}):AddColorPicker("crosshair_outline_color", {
    Default = Color3.new(0,0,0),
    Callback = function(v) ELI.crosshair.outline_color = v end,
})

local ch_lines = crosshair_tabbox:AddTab("lines")
ch_lines:AddToggle("crosshair_dot", {
    Text = "center dot",
    Default = false,
    Callback = function(v) ELI.crosshair.dot = v end,
})
ch_lines:AddToggle("crosshair_top", {
    Text = "top line",
    Default = true,
    Callback = function(v) ELI.crosshair.top_line = v end,
})
ch_lines:AddToggle("crosshair_bottom", {
    Text = "bottom line",
    Default = true,
    Callback = function(v) ELI.crosshair.bottom_line = v end,
})
ch_lines:AddToggle("crosshair_left", {
    Text = "left line",
    Default = true,
    Callback = function(v) ELI.crosshair.left_line = v end,
})
ch_lines:AddToggle("crosshair_right", {
    Text = "right line",
    Default = true,
    Callback = function(v) ELI.crosshair.right_line = v end,
})

local ch_effects = crosshair_tabbox:AddTab("effects")
ch_effects:AddToggle("crosshair_rotating", {
    Text = "rotating",
    Default = false,
    Callback = function(v) ELI.crosshair.rotating = v end,
})
ch_effects:AddSlider("crosshair_rotating_speed", {
    Text = "speed",
    Default = 120, Min = 0, Max = 720, Rounding = 0, Suffix = "°/s",
    Callback = function(v) ELI.crosshair.rotating_speed = v end,
})
ch_effects:AddToggle("crosshair_spread", {
    Text = "spread",
    Default = false,
    Callback = function(v) ELI.crosshair.spread = v end,
})
ch_effects:AddSlider("crosshair_spread_scale", {
    Text = "spread scale",
    Default = 1, Min = 0, Max = 4, Rounding = 2,
    Callback = function(v) ELI.crosshair.spread_scale = v end,
})
ch_effects:AddToggle("crosshair_animation", {
    Text = "animation",
    Default = false,
    Callback = function(v) ELI.crosshair.animation = v end,
})
ch_effects:AddDropdown("crosshair_animation_mode", {
    Text = "mode",
    Default = "pulse",
    Values = { "pulse", "breathe" },
    Callback = function(v) ELI.crosshair.animation_mode = v end,
})
ch_effects:AddSlider("crosshair_animation_speed", {
    Text = "speed",
    Default = 3, Min = 0.1, Max = 12, Rounding = 1,
    Callback = function(v) ELI.crosshair.animation_speed = v end,
})
ch_effects:AddToggle("crosshair_fade", {
    Text = "fade color",
    Default = false,
    Callback = function(v) ELI.crosshair.fade = v end,
}):AddColorPicker("crosshair_fade_color", {
    Default = Color3.fromRGB(255,0,170),
    Callback = function(v) ELI.crosshair.fade_color = v end,
})
ch_effects:AddToggle("crosshair_follow_target", {
    Text = "follow target",
    Default = false,
    Callback = function(v) ELI.crosshair.follow_target = v end,
})
ch_effects:AddDropdown("crosshair_follow_source", {
    Text = "follow source",
    Default = "target",
    Values = { "target", "gunpoint" },
    Callback = function(v) ELI.crosshair.follow_source = v end,
})
ch_effects:AddToggle("crosshair_shimmer", {
    Text = "shimmer",
    Default = false,
    Callback = function(v) ELI.crosshair.shimmer = v end,
})
ch_effects:AddSlider("crosshair_shimmer_speed", {
    Text = "shimmer speed",
    Default = 4, Min = 0.1, Max = 12, Rounding = 1,
    Callback = function(v) ELI.crosshair.shimmer_speed = v end,
})

local ch_text = crosshair_tabbox:AddTab("text")
ch_text:AddToggle("crosshair_text_enable", {
    Text = "enabled",
    Default = false,
    Callback = function(v) ELI.crosshair.text_enable = v end,
}):AddColorPicker("crosshair_text_color", {
    Default = Color3.new(1,1,1),
    Callback = function(v) ELI.crosshair.text_color = v end,
})
ch_text:AddInput("crosshair_text_content", {
    Text = "content",
    Default = "secret",
    Callback = function(v) ELI.crosshair.text_content = v end,
})
ch_text:AddDropdown("crosshair_text_font", {
    Text = "font",
    Default = "UI",
    Values = { "UI", "System", "Plex", "Monospace", "Inconsolata" },
    Callback = function(v) ELI.crosshair.text_font = v end,
})
ch_text:AddSlider("crosshair_text_size", {
    Text = "size",
    Default = 16, Min = 8, Max = 48, Rounding = 0,
    Callback = function(v) ELI.crosshair.text_size = v end,
})
ch_text:AddSlider("crosshair_text_offset", {
    Text = "offset",
    Default = 18, Min = 0, Max = 120, Rounding = 0,
    Callback = function(v) ELI.crosshair.text_offset = v end,
})
ch_text:AddToggle("crosshair_text_outline", {
    Text = "outline",
    Default = true,
    Callback = function(v) ELI.crosshair.text_outline = v end,
})
ch_text:AddToggle("crosshair_text_animation", {
    Text = "animation",
    Default = false,
    Callback = function(v) ELI.crosshair.text_animation = v end,
})

-- =====================================================================
--  VIEWMODEL (left tabbox)
-- =====================================================================
local vm_tabbox = tabs.visual:AddLeftTabbox()

local vm_weapon = vm_tabbox:AddTab("weapon")
vm_weapon:AddToggle("vm_weapon_chams_enable", {
    Text = "weapon chams",
    Default = false,
    Callback = function(v)
        ELI.viewmodel_chams.weapon_chams.enable = v
        Options.vm_weapon_chams_material:SetVisible(v)
        Options.vm_weapon_chams_transparency:SetVisible(v)
    end,
}):AddColorPicker("vm_weapon_chams_color", {
    Title = "color",
    Default = Color3.new(1,1,1),
    Callback = function(v) ELI.viewmodel_chams.weapon_chams.color = v end,
})
vm_weapon:AddDropdown("vm_weapon_chams_material", {
    Text = "material",
    Default = "Neon",
    Values = ELI.material_names,
    Visible = false,
    Callback = function(v) ELI.viewmodel_chams.weapon_chams.material = v end,
})
vm_weapon:AddSlider("vm_weapon_chams_transparency", {
    Text = "transparency",
    Default = 0, Min = 0, Max = 1, Rounding = 2, Visible = false,
    Callback = function(v) ELI.viewmodel_chams.weapon_chams.transparency = v end,
})
vm_weapon:AddToggle("vm_weapon_highlight_enable", {
    Text = "weapon highlight",
    Default = false,
    Callback = function(v) ELI.viewmodel_chams.weapon_highlight.enable = v end,
}):AddColorPicker("vm_weapon_highlight_color", {
    Title = "fill color",
    Default = Color3.new(1,1,1),
    Callback = function(v) ELI.viewmodel_chams.weapon_highlight.color = v end,
}):AddColorPicker("vm_weapon_highlight_outline_color", {
    Title = "outline color",
    Default = Color3.new(1,1,1),
    Callback = function(v) ELI.viewmodel_chams.weapon_highlight.outline_color = v end,
})

local vm_arms = vm_tabbox:AddTab("arms")
vm_arms:AddToggle("vm_arm_chams_enable", {
    Text = "arm chams",
    Default = false,
    Callback = function(v)
        ELI.viewmodel_chams.arm_chams.enable = v
        Options.vm_arm_chams_material:SetVisible(v)
        Options.vm_arm_chams_transparency:SetVisible(v)
        Options.vm_arm_chams_invisible:SetVisible(v)
    end,
}):AddColorPicker("vm_arm_chams_color", {
    Title = "color",
    Default = Color3.new(1,1,1),
    Callback = function(v) ELI.viewmodel_chams.arm_chams.color = v end,
})
vm_arms:AddDropdown("vm_arm_chams_material", {
    Text = "material",
    Default = "Neon",
    Values = ELI.material_names,
    Visible = false,
    Callback = function(v) ELI.viewmodel_chams.arm_chams.material = v end,
})
vm_arms:AddSlider("vm_arm_chams_transparency", {
    Text = "transparency",
    Default = 0, Min = 0, Max = 1, Rounding = 2, Visible = false,
    Callback = function(v) ELI.viewmodel_chams.arm_chams.transparency = v end,
})
vm_arms:AddToggle("vm_arm_chams_invisible", {
    Text = "invisible arms",
    Default = false,
    Visible = false,
    Callback = function(v) ELI.viewmodel_chams.arm_chams.invisible = v end,
})
vm_arms:AddToggle("vm_arm_highlight_enable", {
    Text = "arm highlight",
    Default = false,
    Callback = function(v) ELI.viewmodel_chams.arm_highlight.enable = v end,
}):AddColorPicker("vm_arm_highlight_color", {
    Title = "fill color",
    Default = Color3.new(1,1,1),
    Callback = function(v) ELI.viewmodel_chams.arm_highlight.color = v end,
}):AddColorPicker("vm_arm_highlight_outline_color", {
    Title = "outline color",
    Default = Color3.new(1,1,1),
    Callback = function(v) ELI.viewmodel_chams.arm_highlight.outline_color = v end,
})

-- Viewmodel offsets (right side groupbox)
local vm_offset_box = tabs.visual:AddRightGroupbox("viewmodel offsets")
vm_offset_box:AddToggle("viewmodel_offsets_enable", {
    Text = "viewmodel override",
    Default = false,
    Callback = function(v)
        ELI.viewmodel_offsets.enable = v
        Options.viewmodel_offset_x:SetVisible(v)
        Options.viewmodel_offset_y:SetVisible(v)
        Options.viewmodel_offset_z:SetVisible(v)
    end,
})
vm_offset_box:AddSlider("viewmodel_offset_x", {
    Text = "x", Default = 0, Min = -5, Max = 5, Rounding = 2, Visible = false,
    Callback = function(v) ELI.viewmodel_offsets.x = v end,
})
vm_offset_box:AddSlider("viewmodel_offset_y", {
    Text = "y", Default = 0, Min = -5, Max = 5, Rounding = 2, Visible = false,
    Callback = function(v) ELI.viewmodel_offsets.y = v end,
})
vm_offset_box:AddSlider("viewmodel_offset_z", {
    Text = "z", Default = 0, Min = -5, Max = 5, Rounding = 2, Visible = false,
    Callback = function(v) ELI.viewmodel_offsets.z = v end,
})

task.defer(function()
    task.wait(0.45)
    fetchBg()
    task.wait(0.1)
    stripAlpha(getWin())
    nukeBlue(Library.ScreenGui)
end)

Library:Notify("Secret loaded", 3)
getgenv().Library = Library
